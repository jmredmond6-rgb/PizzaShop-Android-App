<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PizzaShop">

        <activity android:name=".UI.OrderDetailsActivity" />
        <activity android:name=".UI.OrderHistoryActivity" />
        <activity android:name=".UI.CartActivity" />
        <activity android:name=".UI.PizzaMenuActivity" />
        <activity android:name=".UI.RegisterActivity" />

        <activity
            android:name=".UI.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>